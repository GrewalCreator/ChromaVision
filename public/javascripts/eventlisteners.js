var form = document.getElementById("form");
function handleForm(event) { event.preventDefault(); }
form.addEventListener('submit', handleForm);document.addEventListener("submit", uploadBySubmit)
console.log("test")
//TODO: GET .JS FILE LINKING WORKING FIRST